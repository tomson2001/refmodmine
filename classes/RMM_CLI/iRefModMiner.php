<?php
interface iRefModMiner {
	
	//public $_RMM_CLI_COMMAND; // The RefMod-Miner CLI Command (see RefMod-Miner Funktionsbeschreibung at refmod-miner.dfki.de
	//public $_RETURN_DIRECTLY; // Determines, wheter the result is being returned as a file directly or via E-Mail when finished
	
	//public function run();
	
}
?>